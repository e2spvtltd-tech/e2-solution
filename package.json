{
  "name": "e2-solution-backend-deploy",
  "version": "1.0.0",
  "description": "Root configuration for deploying the backend to Railway",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "postinstall": "cd backend && npm install",
    "build": "echo \"No build required for backend\"",
    "start": "cd backend && npm start"
  }
}
